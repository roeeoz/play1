# Work-item breakdown — Improve the search experience

| # | Ref | Title | Tier | Depends on |
|---|---|---|---|---|
| 1 | t1 | Note data model and in-memory store | T1 |  |
| 2 | t2 | Flask HTTP endpoints, integration tests, and p95 latency benchmark | T2 | t1 |

The fenced JSON below is the machine-readable breakdown the platform materializes on approval. Edits must keep it valid JSON with the same schema.

```json
[{"ref":"t1","repoId":"a28764ea-84c4-41c6-9606-7082c2339ee0","title":"Note data model and in-memory store","summary":"## Type\nNew feature — foundational data layer\n\n## Goal\nDeliver the `Note` dataclass and `NoteStore` class that the Flask HTTP layer will consume in t2. This ticket is independently testable with no web framework dependency.\n\n## Files Created / Owned\n- `src/testbed_utils/notes.py` — `Note` dataclass (`id: int`, `title: str`, `body: str`) and `NoteStore` class with `create(title, body) -\u003e Note` (appends to list, increments counter starting at 1) and `search(q) -\u003e list[Note]` (case-insensitive substring match on title and body, returns in insertion order).\n- `tests/test_note_store.py` — pytest unit tests covering: create returns Note with correct sequential id; search matches title substring (case-insensitive); search matches body substring (case-insensitive); search returns empty list when no match; search returns all matching notes in insertion order; fresh store is empty.\n\n## Files Consumed\nNone — this is the foundational ticket.\n\n## Interfaces Touched\n- Introduces `NoteStore` and `Note` as the stable contract that t2 imports.\n\n## Acceptance Criteria\n- `NoteStore().create('My note', 'Some content')` returns `Note(id=1, title='My note', body='Some content')`.\n- A second `create` call returns `Note(id=2, ...)`.\n- `search('NOTE')` matches a note with body `'A note about things'` (case-insensitive).\n- `search('xyz')` returns `[]` when no note contains 'xyz'.\n- Search results appear in note-creation order.\n- A fresh `NoteStore` has no notes.\n- `pytest tests/test_note_store.py` passes; existing test suite is unaffected.\n- No `CI_FAIL` file is created.\n\n## Risk Level\nLow — pure in-memory Python, no framework, no I/O.","dependsOn":[],"complexity":{"scores":{"scope_clarity":0,"technical_design_decisions":0,"system_impact":0,"async_distributed_complexity":0,"validation_complexity":0,"risk_unknowns":0},"justifications":{"scope_clarity":"Fully specified: dataclass fields, sequential integer IDs, list-based storage, case-insensitive substring matching, insertion order — no ambiguity remains.","technical_design_decisions":"No design decisions: dataclass + list is the obvious and only reasonable implementation; counter starts at 1 as resolved by research.","system_impact":"Single new module with no cross-service or cross-component impact; zero changes to existing files.","async_distributed_complexity":"Purely synchronous in-process Python; no async, no threads, no distributed concerns whatsoever.","validation_complexity":"Unit tests only, directly imported; no integration surface, no test client, no external services.","risk_unknowns":"Trivial data model with well-understood Python stdlib primitives; essentially zero risk."},"total_score":0,"tier":"T1","engineer_level":"junior","reasoning_effort":"Very Low","token_budget":"Small","human_decision_required":false}},{"ref":"t2","repoId":"a28764ea-84c4-41c6-9606-7082c2339ee0","title":"Flask HTTP endpoints, integration tests, and p95 latency benchmark","summary":"## Type\nNew feature — HTTP layer, integration tests, performance benchmark, dependency wiring\n\n## Goal\nWire `NoteStore` (from t1) into a Flask application, expose `POST /notes` and `GET /search`, add integration tests via the Flask test client, add a p95 latency benchmark, and update `pyproject.toml` to declare the Flask dependency. This ticket is the composition point that assembles and verifies the full feature end-to-end.\n\n## Files Created / Owned\n- `src/testbed_utils/app.py` — `create_app(store: NoteStore | None = None) -\u003e Flask` factory. Module-level `_store = NoteStore()` singleton used when no store is injected. `POST /notes`: parse JSON body, validate `title` and `body` (missing or empty-string → 400 `{\"field\": \"\u003cname\u003e\", \"error\": \"required and must be non-empty\"}`), call `store.create()`, return 201 `{\"id\", \"title\", \"body\"}`. `GET /search`: extract `q` param (absent → 400 `{\"error\": \"q is required\"}`), call `store.search(q)`, return 200 JSON array (empty array when no matches, never 404).\n- `tests/test_notes_api.py` — integration tests via `app.test_client()` with a fresh `NoteStore` per test: POST with valid body returns 201 and `{id, title, body}` (AC1); POST missing `title` returns 400 naming `title` (AC2); POST with empty `body` returns 400 naming `body` (AC3); GET search returns matching notes case-insensitively (AC4); GET search returns `[]` on no match (AC5); results ordered by insertion (AC6); GET search without `q` returns 400 (OQ2 resolution); title-only match; body-only match; both-field match.\n- `tests/test_bench.py` — seeds store with 10 000 notes, runs `GET /search?q=xyz` 200 times via test client, records per-call latency, asserts `statistics.quantiles(latencies, n=20)[18] \u003c 0.200` (p95 \u003c 200 ms) (AC7).\n\n## Files Modified\n- `pyproject.toml` — adds `flask\u003e=3` to `[project.dependencies]` and to `[project.optional-dependencies] test`.\n\n## Files Consumed (from t1)\n- `src/testbed_utils/notes.py` — imports `Note`, `NoteStore`.\n\n## CI Integration\nThe existing `.github/workflows/ci.yml` runs `pytest tests/` and the `CI_FAIL` gate automatically; all new test files are discovered without any workflow modification. The Flask test client provides the end-to-end smoke for the HTTP surface on every run.\n\n## Acceptance Criteria\n- `POST /notes` with `{\"title\": \"My note\", \"body\": \"Some content\"}` → 201 `{\"id\": 1, \"title\": \"My note\", \"body\": \"Some content\"}` (AC1).\n- `POST /notes` with missing `title` → 400 JSON referencing `title` by name (AC2).\n- `POST /notes` with `body: \"\"` → 400 JSON referencing `body` by name (AC3).\n- `GET /search?q=content` returns 200 array of all notes whose title or body contains 'content', case-insensitive (AC4).\n- `GET /search?q=zzznomatch` returns 200 `[]` (AC5).\n- Search results are in note-creation (insertion) order (AC6).\n- p95 latency of `GET /search` \u003c 200 ms with 10 000 stored notes (AC7).\n- A fresh process (fresh `NoteStore`) starts with zero notes (AC8).\n- `GET /search` (no `q`) → 400 `{\"error\": \"q is required\"}`.\n- Full `pytest` suite passes with no regressions; no `CI_FAIL` file exists.\n\n## Risk Level\nLow-moderate — Flask test client integration and benchmark are well-understood patterns; no novel async or distributed concerns.","dependsOn":["t1"],"complexity":{"scores":{"scope_clarity":0,"technical_design_decisions":1,"system_impact":0,"async_distributed_complexity":0,"validation_complexity":1,"risk_unknowns":0},"justifications":{"scope_clarity":"All endpoint behaviors, status codes, error schemas, and edge cases are fully specified; OQ1 and OQ2 resolved in research.","technical_design_decisions":"App factory pattern, error response JSON schema, and Flask wiring require local design choices — all have one clear reasonable approach but still require judgment.","system_impact":"Single new Flask module; pyproject.toml gains one dependency; no cross-service or multi-team impact.","async_distributed_complexity":"Synchronous Flask with in-process test client; zero async or distributed concerns.","validation_complexity":"Integration testing via Flask test client plus a statistical performance benchmark — more complex than pure unit tests but well within a single test file.","risk_unknowns":"Flask 3.x is stable and well-understood; performance margin is large; no meaningful unknowns remain."},"total_score":2,"tier":"T2","engineer_level":"junior/mid","reasoning_effort":"Low","token_budget":"Medium","human_decision_required":false}}]
```
