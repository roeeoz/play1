# Work-item breakdown — Make the tool faster

| # | Ref | Title | Tier | Depends on |
|---|---|---|---|---|
| 1 | t1 | Optimise slugify and truncate in textutils.py | T1 |  |
| 2 | t2 | Add benchmark regression test tests/test_performance.py | T1 | t1 |

The fenced JSON below is the machine-readable breakdown the platform materializes on approval. Edits must keep it valid JSON with the same schema.

```json
[{"ref":"t1","repoId":"a28764ea-84c4-41c6-9606-7082c2339ee0","title":"Optimise slugify and truncate in textutils.py","summary":"## Type\nPerformance fix — in-place micro-optimisation of two existing functions.\n\n## Goal\nReduce intermediate string allocations in `slugify` (FR1) and eliminate redundant `len()` calls in `truncate` (FR2), with no change to public API or return values.\n\n## Files Owned\n- `src/testbed_utils/textutils.py` — MODIFIED\n\n## Implementation Details\n\n### FR1 — slugify\nReplace the encode→decode line:\n```python\n# before\nascii_text = normalized.encode(\"ascii\", \"ignore\").decode(\"ascii\")\n# after\nascii_text = \"\".join(c for c in normalized if ord(c) \u003c 128)\n```\nThe two module-level compiled regex constants `_SLUG_STRIP_RE` and `_SLUG_COLLAPSE_RE` are retained and applied unchanged.\n\n### FR2 — truncate\nInsert at the top of the function body (before the `ValueError` guard):\n```python\nn = len(text)\nm = len(suffix)\n```\nReplace every subsequent `len(text)` with `n` and every `len(suffix)` with `m` within the function body.\n\n## Acceptance Criteria\n- [ ] `pytest tests/test_textutils.py tests/test_dateutils.py` exits 0; all 24 existing tests pass (AC1, FR5).\n- [ ] `slugify` return values are byte-for-byte identical to the pre-change implementation for every input in the existing test suite (AC6).\n- [ ] `truncate` return values are byte-for-byte identical to the pre-change implementation for every input in the existing test suite (AC7).\n- [ ] `_SLUG_STRIP_RE` and `_SLUG_COLLAPSE_RE` module-level constants are retained (FR1).\n- [ ] No `CI_FAIL` file introduced at repo root (AC3, FR4).\n- [ ] `pyproject.toml` dependencies unchanged (AC4).\n- [ ] Diff ≤ 40 lines across all modified files (AC5).\n\n## Risk Level\nLow — semantically equivalent rewrite of two isolated pure functions verified by the existing 24-test suite.","dependsOn":[],"complexity":{"scores":{"scope_clarity":0,"technical_design_decisions":0,"system_impact":0,"async_distributed_complexity":0,"validation_complexity":0,"risk_unknowns":0},"justifications":{"scope_clarity":"Exact line numbers and replacement code are specified in the spec and confirmed by codebase exploration; no ambiguity remains.","technical_design_decisions":"No design choices remain — the generator expression, variable names, and insertion point are fully prescribed.","system_impact":"Single file, single pure-Python module, no cross-component or cross-service effects.","async_distributed_complexity":"Pure synchronous string operations; no async or distributed concerns whatsoever.","validation_complexity":"Correctness is validated by the existing 24-test suite with no changes required to those tests.","risk_unknowns":"The change is a semantic no-op rewrite confirmed by NFKD decomposition reasoning and existing accent-stripping tests."},"total_score":0,"tier":"T1","engineer_level":"junior","reasoning_effort":"Very Low","token_budget":"Small","human_decision_required":false}},{"ref":"t2","repoId":"a28764ea-84c4-41c6-9606-7082c2339ee0","title":"Add benchmark regression test tests/test_performance.py","summary":"## Type\nNew test file — benchmark regression gate.\n\n## Goal\nAdd `tests/test_performance.py` with two benchmark test functions asserting `slugify` and `truncate` each complete in under 0.1 s on a 10 000-character synthetic Unicode input (FR3). Provides a verifiable artefact for the performance claim and catches future regressions.\n\n## Files Owned\n- `tests/test_performance.py` — CREATED\n\n## Files Consumed (read-only, from t1)\n- `src/testbed_utils/textutils.py` — imports `slugify` and `truncate` (optimised versions from t1)\n\n## Implementation Details\n\n```python\nimport time\nfrom testbed_utils import slugify, truncate\n\n_INPUT = (\"Hello, Wörld! Café 123. \" * 500)[:10000]\n\ndef test_slugify_performance():\n    start = time.perf_counter()\n    slugify(_INPUT)\n    assert time.perf_counter() - start \u003c 0.1\n\ndef test_truncate_performance():\n    start = time.perf_counter()\n    truncate(_INPUT, max_length=5000, suffix=\"...\")\n    assert time.perf_counter() - start \u003c 0.1\n```\n\n- Uses only `time.perf_counter()` from the Python standard library — no third-party timing dependencies.\n- Synthetic input is deterministic; it exercises the NFKD Unicode decomposition path in `slugify`.\n- `max_length=5000` with `suffix=\"...\"` exercises the active truncation branch (not the trivial early-return).\n- File follows the `test_*.py` naming convention and is auto-discovered by pytest via `testpaths = [\"tests\"]` in `pyproject.toml`.\n\n## Acceptance Criteria\n- [ ] `pytest tests/test_performance.py` exits 0 (AC2, FR3).\n- [ ] `test_slugify_performance` asserts slugify on 10 000 chars completes in \u003c 0.1 s.\n- [ ] `test_truncate_performance` asserts truncate on 10 000 chars (max_length=5000, suffix=\"...\") completes in \u003c 0.1 s.\n- [ ] No new entries in `pyproject.toml` dependencies — uses stdlib `time` only (AC4).\n- [ ] File is discoverable by pytest without additional configuration changes.\n- [ ] Full suite `pytest tests/` passes: all 24 existing tests + 2 new benchmark tests (AC1 + AC2).\n- [ ] No `CI_FAIL` file at repo root (AC3).\n\n## Risk Level\nVery low — the 0.1 s threshold is ~20–100× the expected runtime on any modern CPU. Flakiness from CI load is extremely unlikely.","dependsOn":["t1"],"complexity":{"scores":{"scope_clarity":0,"technical_design_decisions":1,"system_impact":0,"async_distributed_complexity":0,"validation_complexity":0,"risk_unknowns":0},"justifications":{"scope_clarity":"Threshold (0.1 s), input size (10 000 chars), and stdlib-only constraint are fully specified; no functional ambiguity.","technical_design_decisions":"Minor local choices required: one vs. two test functions (chose two for independent reporting), exact synthetic input string composition, and timing method selection (perf_counter over time.time).","system_impact":"New file in tests/ only; no changes to source modules or CI configuration files.","async_distributed_complexity":"Pure synchronous test functions; no async or distributed concerns.","validation_complexity":"The test file is itself the validation artefact; pytest discovers and runs it without additional integration setup.","risk_unknowns":"Timing assertions on a very generous threshold are extremely unlikely to produce false negatives on any CI runner."},"total_score":1,"tier":"T1","engineer_level":"junior","reasoning_effort":"Very Low","token_budget":"Small","human_decision_required":false}}]
```
