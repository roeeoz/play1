# Work-item breakdown — business_days_between helper in dateutils

| # | Ref | Title | Tier | Depends on |
|---|---|---|---|---|
| 1 | t1 | Add business_days_between helper to dateutils with package export and pytest coverage | T1 |  |

The fenced JSON below is the machine-readable breakdown the platform materializes on approval. Edits must keep it valid JSON with the same schema.

```json
[{"ref":"t1","repoId":"971bf272-f661-4343-ba13-83764c3aec1f","title":"Add business_days_between helper to dateutils with package export and pytest coverage","summary":"Intent: Add the `business_days_between` helper to the testbed-utils `dateutils` module so the future `date-span` CLI and other callers get a tested Monday–Friday day count between two dates. This is the only repository and it already hosts `dateutils`.\n\nOwns: `src/testbed_utils/dateutils.py` (additive change only — new helper; `days_between`, `is_weekend`, `humanize_delta` unchanged), `src/testbed_utils/__init__.py` (add the new name to the import and to `__all__`, keeping alphabetical order), `tests/test_dateutils.py` (new test class for the helper, following the existing one-class-per-function, plain-assert style). Consumes: existing `is_weekend`/`date` machinery as-is. Touches nothing else — no `pyproject.toml`, README, CI, or CLI changes.\n\nAcceptance criteria:\n- `business_days_between` is importable from both `testbed_utils.dateutils` and the `testbed_utils` package, and accepts two `datetime.date` values, returning an integer.\n- Counting is half-open: the earlier date is counted, the later date excluded. 2024-01-01 (Mon) → 2024-01-08 (Mon) returns 5.\n- 2024-01-06 (Sat) → 2024-01-07 (Sun) returns 0.\n- 2024-01-01 (Mon) → 2024-01-02 (Tue) returns 1.\n- Identical start and end dates return 0.\n- Reversed order returns the same value as chronological order (e.g. 2024-01-08 → 2024-01-01 returns 5); the result is never negative. The docstring states this order-independence explicitly, since the sibling `days_between` is signed.\n- The existing `days_between`, `is_weekend`, and `humanize_delta` functions and their tests are byte-for-byte unchanged.\n- `tests/test_dateutils.py` gains passing tests for all five scenarios above (full week, weekend-only, single business day, identical dates, reversed order) with the exact expected values.\n- `pytest` passes in full (baseline 38 tests plus the new ones); any `\u003e\u003e\u003e` example added to the docstring is hand-verified because doctests are not collected.\n- No new third-party dependency, no version bump, no `date-span` script, `--json` mode, or README change.","dependsOn":[],"complexity":{"scores":{"scope_clarity":0,"technical_design_decisions":0,"system_impact":0,"async_distributed_complexity":0,"validation_complexity":0,"risk_unknowns":0},"justifications":{"scope_clarity":"Five concrete AC values pin the behavior fully; signature and convention are locked in the HLD.","technical_design_decisions":"Follows the existing dateutils helper pattern; only the trivial loop-vs-formula choice is left to the implementer.","system_impact":"Single module in a single package plus its test file; nothing else is touched.","async_distributed_complexity":"Pure synchronous function; no async or distributed behavior.","validation_complexity":"Plain pytest unit tests in the existing test file, no fixtures or integration.","risk_unknowns":"Standard-library-only, additive change with a green baseline suite; the only trap (inclusive vs half-open) is closed by the pinned tests."},"total_score":0,"tier":"T1","engineer_level":"junior","reasoning_effort":"Very Low","token_budget":"Small","human_decision_required":false}}]
```
