# Work-item breakdown — Add a greet(name) helper to testbed_utils

| # | Ref | Title | Tier | Depends on |
|---|---|---|---|---|
| 1 | t1 | Add greet(name) function to testbed_utils with export and test | T1 |  |

The fenced JSON below is the machine-readable breakdown the platform materializes on approval. Edits must keep it valid JSON with the same schema.

```json
[{"ref":"t1","repoId":"a28764ea-84c4-41c6-9606-7082c2339ee0","title":"Add greet(name) function to testbed_utils with export and test","summary":"## Type\nFeature — additive function implementation\n\n## Goal\nShip the public `greet(name)` callable in the `testbed_utils` package, wired through the package's public API, and covered by a passing pytest test.\n\n## Scope\nExactly three files are modified; no other files are touched.\n\n### Files CREATED/OWNED by this ticket\n- `src/testbed_utils/textutils.py` — append `greet` after `word_count`\n- `src/testbed_utils/__init__.py` — add `greet` to the `from testbed_utils.textutils import …` line and insert `\"greet\"` into `__all__` alphabetically between `\"days_between\"` and `\"humanize_delta\"`\n- `tests/test_textutils.py` — add `greet` to the top-of-file import; append a `TestGreet` class\n\n## Implementation Details\n\n**`src/testbed_utils/textutils.py`** — append after `word_count`:\n```python\ndef greet(name: str) -\u003e str:\n    return f\"Hello, {name}!\"\n```\n\n**`src/testbed_utils/__init__.py`** — extend the textutils import line and `__all__`:\n```python\nfrom testbed_utils.textutils import greet, slugify, truncate, word_count\n\n__all__ = [\n    \"days_between\",\n    \"greet\",\n    \"humanize_delta\",\n    \"is_weekend\",\n    \"slugify\",\n    \"truncate\",\n    \"word_count\",\n]\n```\n\n**`tests/test_textutils.py`** — update import on line 1 to include `greet`; append:\n```python\nclass TestGreet:\n    def test_returns_greeting(self):\n        assert greet(\"Roee\") == \"Hello, Roee!\"\n```\n\n## Interfaces Touched\n- `testbed_utils` public API surface (additive only)\n- `testbed_utils.__all__` (additive only)\n\n## Acceptance Criteria\n- `greet(\"Roee\")` returns exactly `\"Hello, Roee!\"` (string equality, comma and exclamation mark).\n- `from testbed_utils import greet` succeeds without error.\n- `pytest` reports the new `TestGreet::test_returns_greeting` test as passing.\n- Running the full `pytest` suite produces no new failures vs. the pre-change baseline.\n- No `CI_FAIL` file is introduced.\n\n## Risk Level\nLow — purely additive, no existing code paths modified, existing test suite remains untouched.","dependsOn":[],"complexity":{"scores":{"scope_clarity":0,"technical_design_decisions":0,"system_impact":0,"async_distributed_complexity":0,"validation_complexity":0,"risk_unknowns":0},"justifications":{"scope_clarity":"Behavior is completely specified: exact return format, exact test assertion, exact export path. No ambiguity.","technical_design_decisions":"Follows the identical pattern of existing helpers (slugify, truncate, word_count) in textutils.py. No design choices remain.","system_impact":"Single package, single module. No cross-service or cross-team impact.","async_distributed_complexity":"Stateless synchronous function with no I/O. Async/distributed concerns are entirely absent.","validation_complexity":"One unit test asserting string equality. Straightforward pytest class following an existing pattern.","risk_unknowns":"Purely additive change to a self-contained utility module. No side effects, no migration, no performance concerns."},"total_score":0,"tier":"T1","engineer_level":"junior","reasoning_effort":"Very Low","token_budget":"Small","human_decision_required":false}}]
```
