# Work-item breakdown — API של ספר אורחים – שמירה והצגת רשומות

| # | Ref | Title | Tier | Depends on |
|---|---|---|---|---|
| 1 | t1 | Guestbook JSON API: Flask service with SQLite storage, validation, tests, and run docs | T3 |  |

The fenced JSON below is the machine-readable breakdown the platform materializes on approval. Edits must keep it valid JSON with the same schema.

```json
[{"ref":"t1","repoId":"971bf272-f661-4343-ba13-83764c3aec1f","title":"Guestbook JSON API: Flask service with SQLite storage, validation, tests, and run docs","summary":"**Intent:** Deliver play1's first HTTP surface: a dependency-light Flask JSON API that creates guestbook entries and lists them newest-to-oldest, persisted in SQLite via the stdlib, with a stable per-entry id so s3 can delete by id later. This repo is the only target and holds the existing `testbed_utils` package the service lives alongside.\n\n**Owns:** new package `src/guestbook/` (all modules inside it: app factory/routes, storage, validation, run entry point); new `tests/test_guestbook_api.py`; edits to `pyproject.toml` (add `flask` to `dependencies`, add a `guestbook` entry under `[project.scripts]`); a new README section on running and testing the API. **Consumes (read-only, must not modify):** `src/testbed_utils/**`, existing `tests/test_*.py`, `.github/workflows/ci.yml`. Must not create a `CI_FAIL` file.\n\n**Acceptance criteria:**\n- `POST /api/guestbook/entries` with JSON body containing `author` (1–50 characters after trimming) and `message` (1–300 characters after trimming) returns 201 with a JSON object containing `id` (stable unique integer), `author`, `message`, and `created_at`; `created_at` is set by the server as ISO-8601 UTC ending in `Z`, and any client-supplied `created_at` is ignored. Stored values are the trimmed strings.\n- `POST` with `author` or `message` missing, not a string, empty, whitespace-only, or over the length limit returns 400 with body `{\"error\": {\"code\": \"validation_error\", \"fields\": {...}}}` where `fields` lists only the failing field names with a human-readable message each; a subsequent `GET` shows no new entry.\n- `POST` with a non-JSON or non-object body returns 400 with the same envelope and `code` `invalid_json`, and nothing is stored.\n- `GET /api/guestbook/entries` returns 200 with a JSON array; each element has `id`, `author`, `message`, `created_at`.\n- The array is ordered newest-to-oldest deterministically: by creation time descending, then by id descending, so entries created within the same clock tick keep a stable order across repeated calls.\n- `GET` with no entries returns 200 and `[]` (not null, not an error).\n- Creating 100 entries then calling `GET` returns all 100 in one response, in the specified order, with no truncation.\n- Entries persist in a SQLite database file whose path is configurable (environment variable) and defaults to a location already covered by `.gitignore` (e.g. under `instance/`); the app works correctly under Flask's threaded dev server (per-request connection to the file, or a shared connection with locking) and a test covers concurrent-safe creates or at least the threaded configuration path.\n- Tests in `tests/test_guestbook_api.py` use Flask's built-in test client against an isolated temporary database per test, cover every criterion above, and the full suite (`pytest`) passes along with the existing 38 tests; no new packages beyond `flask` and its transitive dependencies are added, and `pip install -e \".[test]\"` (what CI runs) installs everything the tests need.\n- After `pip install -e .`, running the `guestbook` console script starts a local dev server serving both endpoints; README gains a short section documenting the two endpoints, the error envelope, the database path variable, and how to run and test.\n- `src/testbed_utils/` and its existing tests are unchanged; the CI workflow is unchanged.","dependsOn":[],"complexity":{"scores":{"scope_clarity":0,"technical_design_decisions":1,"system_impact":0,"async_distributed_complexity":0,"validation_complexity":1,"risk_unknowns":1},"justifications":{"scope_clarity":"Endpoints, status codes, JSON shapes, validation rules, ordering, and error envelope are fully specified in the HLD contract.","technical_design_decisions":"Local choices remain: Flask app-factory layout, SQLite connection strategy under a threaded dev server, test-isolation approach.","system_impact":"Single new package in one repo; no existing component is modified beyond pyproject and README.","async_distributed_complexity":"Purely synchronous request/response; no background work or messaging.","validation_complexity":"Integration-style tests through the Flask test client against a real SQLite file, including a 100-entry ordering test and threaded-safety coverage.","risk_unknowns":"Moderate: first HTTP surface in the repo, SQLite thread-safety pitfalls, and the response contract freezes for two parallel in-flight stories."},"total_score":3,"tier":"T3","engineer_level":"mid","reasoning_effort":"Medium","token_budget":"Large","human_decision_required":false}}]
```
