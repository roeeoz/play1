# Work-item breakdown — Add a Hello World bash script in a new folder

| # | Ref | Title | Tier | Depends on |
|---|---|---|---|---|
| 1 | t1 | Add executable scripts/hello-world/hello_world.sh printing Hello, World! | T1 |  |

The fenced JSON below is the machine-readable breakdown the platform materializes on approval. Edits must keep it valid JSON with the same schema.

```json
[{"ref":"t1","repoId":"0fcb6992-968b-4373-a63d-0575b37433ef","title":"Add executable scripts/hello-world/hello_world.sh printing Hello, World!","summary":"Intent: Deliver the single new bash script that prints the greeting and exits 0, placed in a new folder at the repository root, so the delivery pipeline can be exercised end to end. roeeoz/play1 is the only repository in scope.\n\nOwns / Consumes: Owns (creates) `scripts/hello-world/` and `scripts/hello-world/hello_world.sh`. Consumes nothing. Must not touch any other file in the repository.\n\nAcceptance criteria:\n- A folder `scripts/hello-world/` exists at the repository root.\n- A file `scripts/hello-world/hello_world.sh` exists inside that folder with a `#!/bin/bash` first line and LF line endings only.\n- Running `./scripts/hello-world/hello_world.sh` from the repository root writes exactly `Hello, World!` followed by one newline to stdout, with nothing written to stderr.\n- Running `bash scripts/hello-world/hello_world.sh` produces the identical stdout, empty stderr.\n- Both invocations exit with status code 0.\n- The file has the executable bit set in the working tree (`ls -l` shows `x`), and the committed git mode is `100755` (verifiable with `git ls-files -s scripts/hello-world/hello_world.sh`).\n- The PR diff adds exactly one file and modifies or removes no other file (no README, CI, pyproject, src, tests, or docs changes).\n- The PR description includes the verification commands run and their observed output (stdout bytes, exit code, and committed file mode).","dependsOn":[],"complexity":{"scores":{"scope_clarity":0,"technical_design_decisions":0,"system_impact":0,"async_distributed_complexity":0,"validation_complexity":0,"risk_unknowns":0},"justifications":{"scope_clarity":"Path, filename, exact output, exit code, and file mode are all fixed by the approved spec.","technical_design_decisions":"Shebang is fixed by the spec; a fixed-string echo is the only construct needed.","system_impact":"One new standalone file, invisible to pytest, packaging, and CI.","async_distributed_complexity":"Synchronous single-process script with no async behaviour.","validation_complexity":"Manual run of two invocations plus a git mode check; no test harness needed.","risk_unknowns":"Only risk is committing mode 100644 or CRLF, both caught by the stated pre-push checks."},"total_score":0,"tier":"T1","engineer_level":"junior","reasoning_effort":"Very Low","token_budget":"Small","human_decision_required":false}}]
```
