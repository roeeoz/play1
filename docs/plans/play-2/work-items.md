# Work-item breakdown — Add sentence_count and reading_time_minutes helpers to textutils

| # | Ref | Title | Tier | Depends on |
|---|---|---|---|---|
| 1 | t1 | Add sentence_count and reading_time_minutes helpers to textutils with unit tests | T1 |  |

The fenced JSON below is the machine-readable breakdown the platform materializes on approval. Edits must keep it valid JSON with the same schema.

```json
[{"ref":"t1","repoId":"971bf272-f661-4343-ba13-83764c3aec1f","title":"Add sentence_count and reading_time_minutes helpers to textutils with unit tests","summary":"Intent: Deliver the two pure text-statistics helpers that the later text-stats CLI story will consume, placed next to word_count in the textutils module of testbed-utils, with pytest coverage for every acceptance scenario. This repo is the only repo in scope and owns the textutils module.\n\nOwns / Consumes: OWNS src/testbed_utils/textutils.py (appends two functions after word_count; does not modify existing functions), src/testbed_utils/__init__.py (extends the textutils import line and __all__ in alphabetical order), and tests/test_textutils.py (extends the import at line 1 and appends two TestX classes after TestWordCount). CONSUMES the existing word_count helper and module conventions as-is. Does NOT touch pyproject.toml, README.md, pdfutils.py, or .github/workflows/ci.yml.\n\nAcceptance criteria:\n- sentence_count(\"Is this fast? Yes! It works.\") returns 3.\n- sentence_count on a string containing words but none of \".\", \"!\", \"?\" returns 0.\n- Each \".\", \"!\", \"?\" character counts individually, so a run such as \"...\" contributes 3; no abbreviation or decimal awareness is added.\n- reading_time_minutes(200) returns 1.\n- reading_time_minutes(450) returns 3.\n- reading_time_minutes(10) returns 1.\n- reading_time_minutes(0) returns 1.\n- Both helpers are defined in src/testbed_utils/textutils.py after word_count, follow the module's existing style (from __future__ annotations, full type hints, one-line summary docstring, a \u003e\u003e\u003e doctest example, stdlib-only, no syntax newer than Python 3.10), and perform no file I/O, CLI, or packaging side effects.\n- Both helpers are importable from testbed_utils.textutils and from the testbed_utils package root, and both names appear in __all__ in alphabetical position.\n- tests/test_textutils.py gains a TestSentenceCount class and a TestReadingTimeMinutes class, each using plain assert methods in the existing per-function style, together covering all six scenarios above.\n- Running pytest from the repo root passes with all 38 existing tests still green plus the new tests; no existing test or function is modified.","dependsOn":[],"complexity":{"scores":{"scope_clarity":0,"technical_design_decisions":0,"system_impact":0,"async_distributed_complexity":0,"validation_complexity":0,"risk_unknowns":0},"justifications":{"scope_clarity":"Six exact input/output pairs are specified; the algorithm (count three characters; ceil divide by 200 with floor 1) is fully stated.","technical_design_decisions":"Follows the existing word_count pattern in the same file; the only choices are trivial expression style.","system_impact":"Single module in a single package, purely additive, no existing call sites affected.","async_distributed_complexity":"Pure synchronous in-memory functions.","validation_complexity":"Plain pytest unit tests in the existing TestX class style; no fixtures, integration, or I/O.","risk_unknowns":"Stdlib-only, no version-specific behavior, 38 existing tests remain untouched."},"total_score":0,"tier":"T1","engineer_level":"junior","reasoning_effort":"Very Low","token_budget":"Small","human_decision_required":false}}]
```
