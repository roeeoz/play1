# Work-item breakdown — Pure-Python number summary helpers (numutils)

| # | Ref | Title | Tier | Depends on |
|---|---|---|---|---|
| 1 | t1 | Add numutils module (mean, median, percentile) with tests and package re-export | T1 |  |

The fenced JSON below is the machine-readable breakdown the platform materializes on approval. Edits must keep it valid JSON with the same schema.

```json
[{"ref":"t1","repoId":"971bf272-f661-4343-ba13-83764c3aec1f","title":"Add numutils module (mean, median, percentile) with tests and package re-export","summary":"Intent: Deliver US-01 in full — a stdlib-only numeric-summary module in the testbed_utils package with three-case pytest coverage per function and docstring examples in the repo's established style. roeeoz/play1 is the only repo and the package lives there under src/.\n\nOwns: src/testbed_utils/numutils.py (new), tests/test_numutils.py (new), src/testbed_utils/__init__.py (edit: import and __all__ entries for the three helpers; package docstring may mention numbers). Consumes: existing style/conventions in src/testbed_utils/textutils.py and dateutils.py and tests/test_textutils.py (read only). Does NOT touch pyproject.toml, CI, or any other module.\n\nAcceptance criteria:\n- mean([1, 2, 3, 4]) == 2.5; mean([7]) == 7; mean([]) raises ValueError whose message states the list must not be empty.\n- median([3, 1, 2]) == 2; median([1, 2, 3, 4]) == 2.5; median([]) raises ValueError whose message states the list must not be empty.\n- percentile([1, 2, 3, 4, 5], 90) == 4.6 (linear interpolation between nearest ranks); percentile([1, 2, 3, 4, 5], 0) == 1 and percentile([1, 2, 3, 4, 5], 100) == 5; percentile([], p) for any valid p raises ValueError whose message states the list must not be empty; percentile([1, 2, 3], 150) raises ValueError whose message states p must be between 0 and 100.\n- percentile also yields 28 for [10, 20, 30] at p=90 and 42 for [42] at p=90 (values US-02 depends on).\n- Every public function in numutils.py has a docstring with a one-line imperative summary and at least one \u003e\u003e\u003e example with literal expected output, matching textutils.py/dateutils.py style; the examples are literally correct, verified by the test file running the module's docstring examples.\n- tests/test_numutils.py follows the existing layout (one Test\u003cFunction\u003e class per function, plain asserts, pytest.raises(ValueError) with message checks) and covers happy path, one boundary and one error case for each of the three functions using in-memory lists only.\n- from testbed_utils import mean, median, percentile works and the three names appear in testbed_utils.__all__.\n- Module uses only the standard library; no change to pyproject.toml dependencies, no new files outside the owned set; full pytest suite passes on Python 3.10+ (CI runs 3.12).","dependsOn":[],"complexity":{"scores":{"scope_clarity":0,"technical_design_decisions":0,"system_impact":0,"async_distributed_complexity":0,"validation_complexity":0,"risk_unknowns":0},"justifications":{"scope_clarity":"Ten concrete input/output scenarios pin every behaviour; message substance and path are settled.","technical_design_decisions":"Follows the existing textutils/dateutils pattern; the percentile formula is verified against all worked examples.","system_impact":"One new module plus a two-line re-export in the same package.","async_distributed_complexity":"Pure synchronous functions, no I/O.","validation_complexity":"Plain pytest unit tests over in-memory lists.","risk_unknowns":"Stdlib only, exact float results verified, no external surface."},"total_score":0,"tier":"T1","engineer_level":"junior","reasoning_effort":"Very Low","token_budget":"Small","human_decision_required":false}}]
```
