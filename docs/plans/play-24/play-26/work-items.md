# Work-item breakdown — Number summary console script (testbed-num-summary)

| # | Ref | Title | Tier | Depends on |
|---|---|---|---|---|
| 1 | t1 | Add testbed-num-summary console script (numsummary module, pyproject entry, CLI tests) | T2 |  |

The fenced JSON below is the machine-readable breakdown the platform materializes on approval. Edits must keep it valid JSON with the same schema.

```json
[{"ref":"t1","repoId":"971bf272-f661-4343-ba13-83764c3aec1f","title":"Add testbed-num-summary console script (numsummary module, pyproject entry, CLI tests)","summary":"Intent: deliver the complete `testbed-num-summary` console script for US-02 in roeeoz/play1 — the only repo — as one reviewable PR: a new CLI module built on the merged numutils helpers, its console-script wiring, and pytest coverage of every acceptance criterion.\n\nOwns / Consumes: OWNS new `src/testbed_utils/numsummary.py`, the new `testbed-num-summary` line in `pyproject.toml` `[project.scripts]`, new `tests/test_num_summary_cli.py`, and new text fixtures under `tests/fixtures/` (e.g. `numbers.txt` = \"1 2 3 4 5\", `single.txt` = \"42\", whitespace-only `empty.txt`). CONSUMES `src/testbed_utils/numutils.py` (mean, median, percentile) read-only; follows the CLI and test-harness conventions in `src/testbed_utils/pdfutils.py` and `tests/test_pdf_extract_cli.py`. MUST NOT modify `numutils.py`, `tests/test_numutils.py`, `__init__.py`, CI, or dependency lists.\n\nAcceptance criteria:\n- The console script `testbed-num-summary` is registered in `pyproject.toml` `[project.scripts]` pointing at the new module's entry point; `pip install -e \".[test]\"` exposes the command. No runtime or test dependency is added.\n- Running with the path of a fixture containing \"1 2 3 4 5\" prints exactly six stdout lines in order: `count: 5`, `min: 1`, `max: 5`, `mean: 3`, `median: 3`, `p90: 4.6`; nothing on stderr; exit 0.\n- Running with no path argument and \"10\\n20\\n30\\n\" on (simulated) stdin prints exactly `count: 3`, `min: 10`, `max: 30`, `mean: 20`, `median: 20`, `p90: 28`; exit 0.\n- Running with the path of a fixture containing \"42\" prints exactly `count: 1`, `min: 42`, `max: 42`, `mean: 42`, `median: 42`, `p90: 42`; exit 0.\n- Value formatting: integral results print with no decimal part; non-integral results print using Python's default float string form. Tokens that are integers parse as integers; other numeric tokens as floats. Statistics are computed via numutils mean/median/percentile (p=90), not reimplemented.\n- Running with a fixture containing no numbers (empty or whitespace-only), or with empty stdin, prints exactly one stderr line stating no numbers were found, prints nothing to stdout, and exits with a non-zero status (1, matching pdf-extract).\n- Tokens that are not plain numbers (including `nan`/`inf`) are ignored; a file containing only such tokens is treated as empty input.\n- Path safety: before opening, the supplied path is fully resolved (symlinks followed, `..` collapsed) and compared against the fully resolved current working directory at invocation. An absolute path outside cwd (e.g. `/etc/passwd`) and a relative path that traverses outside (e.g. `../x.txt`, `sub/../../x.txt`) are refused without being opened: exactly one stderr line stating the path is not allowed (wording distinct from the empty-input message), nothing on stdout, non-zero exit. A symlink inside cwd resolving outside cwd is likewise refused.\n- A relative path containing `..` whose resolved location is still inside cwd (e.g. `sub/../numbers.txt`) is accepted and summarized normally.\n- File content is only ever read as text; it is never executed or imported.\n- A path inside cwd that cannot be opened (missing file or a directory) yields exactly one `Error:`-prefixed stderr line, nothing on stdout, exit 1.\n- `--help` with no other arguments prints help text to stdout whose description is a single-line summary of the script's purpose (mentioning the statistics it prints) and exits 0.\n- Tests use only fixture files under `tests/fixtures/`, `tmp_path` with `monkeypatch.chdir`, and patched `sys.stdin`; no real terminal, network, or clock. Containment tests pass on both macOS (symlinked tmp dirs) and Linux CI (Python 3.12).\n- Existing tests (including `tests/test_numutils.py` public-surface and doctest checks) continue to pass; full `pytest` run is green. Total diff stays under 300 changed lines.","dependsOn":[],"complexity":{"scores":{"scope_clarity":0,"technical_design_decisions":1,"system_impact":0,"async_distributed_complexity":0,"validation_complexity":0,"risk_unknowns":0},"justifications":{"scope_clarity":"Seven acceptance criteria with exact output lines; residual gaps (mixed tokens, nan/inf, missing file, help shape) are pinned in the HLD.","technical_design_decisions":"Local choices only: formatting rule, token parsing, containment check details; overall pattern copied from pdfutils.py.","system_impact":"One new module plus one pyproject line in a single package; nothing else touched.","async_distributed_complexity":"Synchronous single-process CLI.","validation_complexity":"Unit-style pytest with fixtures, tmp_path/monkeypatch.chdir and patched stdin, reusing the existing run_main harness pattern.","risk_unknowns":"Behaviour verified against the helpers on the exact AC inputs; only cross-platform symlink resolution needs care and is already characterised."},"total_score":1,"tier":"T2","engineer_level":"junior/mid","reasoning_effort":"Low","token_budget":"Medium","human_decision_required":false}}]
```
