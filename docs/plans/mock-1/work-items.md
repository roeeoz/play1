# Work-item breakdown — Add a --json output flag to the CLI

| # | Ref | Title | Tier | Depends on |
|---|---|---|---|---|
| 1 | t1 | Add testbed-utils CLI entry point with --json support | T2 |  |

The fenced JSON below is the machine-readable breakdown the platform materializes on approval. Edits must keep it valid JSON with the same schema.

```json
[{"ref":"t1","repoId":"a28764ea-84c4-41c6-9606-7082c2339ee0","title":"Add testbed-utils CLI entry point with --json support","summary":"## Type\nNew feature — complete vertical slice: packaging wiring, CLI implementation, and tests.\n\n## Goal\nShip the `testbed-utils` command-line entry point that wraps all six exported library functions as subcommands, with a global `--json` flag for machine-readable output, satisfying all nine acceptance criteria from the spec.\n\n## Scope\nAll changes are in the `play1` repository. This ticket is the sole PR for this feature.\n\n**Files CREATED by this ticket:**\n- `src/testbed_utils/cli.py` — full CLI implementation\n- `tests/test_cli.py` — pytest test suite\n\n**Files EDITED by this ticket:**\n- `pyproject.toml` — add `[project.scripts]` table\n\nNo changes to `dateutils.py`, `textutils.py`, or `__init__.py`.\n\n## Implementation Details\n\n### 1. `pyproject.toml`\nAdd:\n```toml\n[project.scripts]\ntestbed-utils = \"testbed_utils.cli:main\"\n```\nNo new `[project.dependencies]` entry — argparse is stdlib.\n\n### 2. `src/testbed_utils/cli.py`\n\n**`CustomParser(ArgumentParser)`** — overrides `error(self, message)`: checks `'--json' in sys.argv`; if true, prints `json.dumps({\"error\": message})` to stdout and calls `sys.exit(1)`; otherwise calls `super().error(message)` (argparse default: print to stderr, exit 2).\n\n**Root parser:** defines `--json` (store_true) then `add_subparsers(dest='subcommand', required=True)`.\n\n**Six subparsers** (kebab-case names):\n- `days-between`: two positional args `start`, `end` (strings; coerced via `date.fromisoformat`)\n- `humanize-delta`: one positional arg `delta` (string; coerced via `timedelta(seconds=int(arg))`)\n- `is-weekend`: one positional arg `day` (string; coerced via `date.fromisoformat`)\n- `slugify`: one positional arg `text` (string; pass-through)\n- `truncate`: positional `text` (str), positional `max_length` (str→int), optional positional `suffix` (`nargs='?'`, default `'...'`)\n- `word-count`: one positional arg `text` (string; pass-through)\n\n**Dispatch + error handling:**\n```python\ndef _emit_error(msg, json_mode):\n    if json_mode:\n        print(json.dumps({\"error\": msg}))\n        sys.exit(1)\n    else:\n        print(f\"error: {msg}\", file=sys.stderr)\n        sys.exit(1)\n\ndef main():\n    parser = CustomParser(...)\n    # ... subparsers ...\n    args = parser.parse_args()\n    json_mode = args.json\n    try:\n        result = _dispatch(args)  # coerce + call library function\n    except (ValueError, Exception) as e:\n        _emit_error(str(e), json_mode)\n    if json_mode:\n        print(json.dumps({\"result\": result}))\n    else:\n        print(str(result))\n```\n\n`CustomParser` stores `json_mode = '--json' in sys.argv` on `self` at construction time so `error()` can reference it without requiring a completed parse.\n\n### 3. `tests/test_cli.py`\n\nClass-based pytest (matching existing test style). All tests use `subprocess.run([\"testbed-utils\", ...], capture_output=True, text=True)`.\n\n- `class TestPlainText`: one test per subcommand — correct args, assert `proc.returncode == 0`, assert expected value in `proc.stdout`, assert `proc.stderr == \"\"`\n- `class TestJsonOutput`: one test per subcommand with `--json` prefix — assert `proc.returncode == 0`, assert `json.loads(proc.stdout)[\"result\"] == expected` with correct Python type\n- `class TestErrors`: wrong arg count with `--json` → assert `proc.returncode == 1`, `json.loads(proc.stdout)[\"error\"]` is non-empty string; wrong arg count without `--json` → assert `proc.returncode == 1`, `proc.stdout == \"\"`, `proc.stderr` non-empty\n- `class TestHelp`: `testbed-utils --help` → exit 0; `testbed-utils days-between --help` → exit 0\n\n## Interfaces Touched\n- `pyproject.toml` `[project.scripts]` (new section)\n- `testbed_utils.__init__` (imported, not modified)\n- `testbed_utils.dateutils.days_between`, `humanize_delta`, `is_weekend` (consumed)\n- `testbed_utils.textutils.slugify`, `truncate`, `word_count` (consumed)\n\n## Acceptance Criteria\n- [ ] AC1: `pip install -e .` makes `testbed-utils` available on PATH\n- [ ] AC2: All six subcommands accept correct arguments, print correct result to stdout, exit 0\n- [ ] AC3: `testbed-utils --json \u003csubcommand\u003e \u003cargs\u003e` writes valid JSON to stdout; `jq .result` extracts value\n- [ ] AC4: JSON success envelope is `{\"result\": \u003cvalue\u003e}` with native JSON type (number/string/boolean)\n- [ ] AC5: Any error under `--json` produces `{\"error\": \"\u003cmessage\u003e\"}` on stdout and exits 1\n- [ ] AC6: Any error without `--json` writes to stderr, nothing to stdout, exits 1\n- [ ] AC7: `testbed-utils --help` and `testbed-utils \u003csubcommand\u003e --help` print usage and exit 0\n- [ ] AC8: All existing and new pytest tests pass; no `CI_FAIL` file introduced; CI gate remains green\n- [ ] AC9: Tests cover both default and `--json` output for each subcommand, and error path under `--json`\n\n## Risk Level\nLow-medium. The one non-obvious detail is argparse error interception; the `'--json' in sys.argv` check in `CustomParser.error()` is the correct mitigation.\n\n## Why this repo\nThe entire feature lives in `play1` — it extends the existing `testbed_utils` package with a new `cli.py` module and entry-point wiring.","dependsOn":[],"complexity":{"scores":{"scope_clarity":0,"technical_design_decisions":1,"system_impact":0,"async_distributed_complexity":0,"validation_complexity":1,"risk_unknowns":1},"justifications":{"scope_clarity":"Spec fully defines behavior, argument types, output envelopes, and error messages with concrete examples; no ambiguity remains after research findings resolved the open questions.","technical_design_decisions":"Requires a custom ArgumentParser subclass for error interception under --json, per-subcommand type coercion strategy, and optional-positional handling for truncate's suffix parameter — local design decisions with well-known solutions.","system_impact":"Self-contained within a single package; no cross-service, cross-repo, or cross-team impact.","async_distributed_complexity":"Fully synchronous CLI; no async, threading, or distributed concerns whatsoever.","validation_complexity":"Tests must invoke the real installed entry point via subprocess to verify AC1 (entry point on PATH); plain unit tests calling Python functions directly would not satisfy the acceptance criteria.","risk_unknowns":"Argparse error() fires before full parse so --json may not be in the namespace; mitigated by checking sys.argv directly, but this is the one non-obvious implementation detail."},"total_score":3,"tier":"T2","engineer_level":"mid","reasoning_effort":"Medium","token_budget":"Medium","human_decision_required":false}}]
```
